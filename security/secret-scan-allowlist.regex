# One regex pattern per line.
# Comments start with '#'.
# Patterns are matched against full finding lines:
#   <absolute-path>:<line>:<content>
#
# Keep this file minimal.
# Any new allowlist entry must include a rationale in `security/SECRET.SCAN.md`.
