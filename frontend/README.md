# Frontend Development Mode

This folder is pure static frontend.

- Open `frontend/index.html` directly in your browser.
- Open `frontend/dashboard.html` directly in your browser.
- No API server is required for this mode.

The pages use:

- `frontend/styles.css`
- `frontend/app.js`
- `img/baseline logo.png` as the header logo
